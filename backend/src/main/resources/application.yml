spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/pasoor}
    username: ${DATABASE_USERNAME:pasoor}
    password: ${DATABASE_PASSWORD:pasoor}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

